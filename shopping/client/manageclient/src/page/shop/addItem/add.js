import React from "react";
import { Card, message, Form, Input, Icon, Button } from "antd";
const { TextArea } = Input;
export default class AddShop extends React.Component {
  render() {
    return <Card title="新增"></Card>;
  }
}
